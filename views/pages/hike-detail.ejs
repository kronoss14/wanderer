<!-- DETAIL HERO -->
<section class="detail-hero">
  <div class="detail-hero-bg" style="background-image: url('<%= hike.heroImage %>')"></div>
  <div class="detail-hero-overlay"></div>
  <div class="detail-hero-content container">
    <a href="/hikes" class="back-link">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="19" y1="12" x2="5" y2="12"/><polyline points="12 19 5 12 12 5"/></svg>
      Back to Hikes
    </a>
    <h1><%= hike.name %></h1>
    <p class="detail-region"><%= hike.region %>, Georgia</p>
  </div>
</section>

<!-- QUICK INFO BAR -->
<section class="quick-info-bar">
  <div class="container">
    <div class="quick-info-grid">
      <div class="quick-info-item">
        <span class="quick-info-label">Duration</span>
        <span class="quick-info-value"><%= hike.duration %></span>
      </div>
      <div class="quick-info-item">
        <span class="quick-info-label">Distance</span>
        <span class="quick-info-value"><%= hike.distance %></span>
      </div>
      <div class="quick-info-item">
        <span class="quick-info-label">Difficulty</span>
        <span class="quick-info-value"><%= hike.difficulty %></span>
      </div>
      <div class="quick-info-item">
        <span class="quick-info-label">Elevation</span>
        <span class="quick-info-value"><%= hike.elevation %></span>
      </div>
      <div class="quick-info-item">
        <span class="quick-info-label">Group Size</span>
        <span class="quick-info-value"><%= hike.groupSize %></span>
      </div>
      <div class="quick-info-item">
        <span class="quick-info-label">Price</span>
        <span class="quick-info-value price">From $<%= hike.price %></span>
      </div>
    </div>
  </div>
</section>

<!-- DETAIL CONTENT -->
<section class="section">
  <div class="container">
    <div class="detail-layout">
      <div class="detail-main">
        <h2 class="detail-section-title">About This Hike</h2>
        <p class="detail-description"><%= hike.description %></p>

        <h2 class="detail-section-title">Highlights</h2>
        <ul class="detail-list highlights-list">
          <% hike.highlights.forEach(item => { %>
            <li>
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="var(--accent-emerald)" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg>
              <%= item %>
            </li>
          <% }) %>
        </ul>

        <div class="detail-includes-grid">
          <div>
            <h2 class="detail-section-title">What's Included</h2>
            <ul class="detail-list included-list">
              <% hike.included.forEach(item => { %>
                <li>
                  <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="var(--accent-emerald)" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg>
                  <%= item %>
                </li>
              <% }) %>
            </ul>
          </div>
          <div>
            <h2 class="detail-section-title">Not Included</h2>
            <ul class="detail-list not-included-list">
              <% hike.notIncluded.forEach(item => { %>
                <li>
                  <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="var(--text-muted)" stroke-width="2"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
                  <%= item %>
                </li>
              <% }) %>
            </ul>
          </div>
        </div>
      </div>
      <aside class="detail-sidebar">
        <div class="booking-widget">
          <h3>Book This Hike</h3>
          <p class="widget-price">From <strong>$<%= hike.price %></strong> per person</p>
          <a href="/booking?hike=<%= hike.id %>" class="btn btn-primary" style="width:100%;justify-content:center;">Book Now</a>
          <a href="/contact" class="btn btn-secondary" style="width:100%;justify-content:center;margin-top:0.75rem;">Ask a Question</a>
        </div>
      </aside>
    </div>
  </div>
</section>

<!-- IMAGE GALLERY STRIP -->
<section class="section section-dark">
  <div class="container">
    <h2 class="section-title text-center" style="margin-bottom: 2rem;">Photo <span class="text-accent">Gallery</span></h2>
    <div class="detail-gallery-strip">
      <% hike.images.forEach(img => { %>
        <div class="detail-gallery-img">
          <img src="<%= img %>" alt="<%= hike.name %>" loading="lazy">
        </div>
      <% }) %>
    </div>
  </div>
</section>

<!-- REVIEWS FOR THIS HIKE -->
<% if (hikeReviews.length > 0) { %>
<section class="section">
  <div class="container">
    <h2 class="section-title text-center" style="margin-bottom: 2rem;">Hiker <span class="text-accent">Reviews</span></h2>
    <div class="review-grid">
      <% hikeReviews.forEach(review => { %>
        <%- include('../partials/_review-card', { review, hikeName: '' }) %>
      <% }) %>
    </div>
  </div>
</section>
<% } %>
