<!-- PAGE HERO -->
<section class="page-hero">
  <div class="page-hero-bg" style="background-image: url('https://picsum.photos/seed/hikes-hero/1920/600')"></div>
  <div class="page-hero-overlay"></div>
  <div class="page-hero-content container">
    <h1>Our <span class="text-accent">Hikes</span></h1>
    <p>Day trips, multi-day treks, and cultural explorations across Georgia's magnificent landscapes.</p>
  </div>
</section>

<!-- FILTER + GRID -->
<section class="section">
  <div class="container">
    <div class="filter-bar">
      <a href="/hikes" class="filter-btn<%= activeType === 'all' ? ' active' : '' %>">All</a>
      <a href="/hikes?type=day" class="filter-btn<%= activeType === 'day' ? ' active' : '' %>">Day Hikes</a>
      <a href="/hikes?type=multi-day" class="filter-btn<%= activeType === 'multi-day' ? ' active' : '' %>">Multi-Day Treks</a>
      <a href="/hikes?type=cultural" class="filter-btn<%= activeType === 'cultural' ? ' active' : '' %>">Cultural Tours</a>
    </div>
    <% if (hikes.length === 0) { %>
      <p class="text-center" style="color: var(--text-secondary); padding: 3rem 0;">No hikes found for this category.</p>
    <% } else { %>
      <div class="hike-grid">
        <% hikes.forEach(hike => { %>
          <%- include('../partials/_hike-card', { hike }) %>
        <% }) %>
      </div>
    <% } %>
  </div>
</section>
